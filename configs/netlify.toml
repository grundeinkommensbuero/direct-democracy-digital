# Based on: https://docs.netlify.com/configure-builds/file-based-configuration/
# Settings in the [build] context are global and are applied to all contexts
# unless otherwise overridden by more specific contexts.
[build]
# Directory to change to before starting a build.
# This is where we will look for package.json/.nvmrc/etc.
# If not set, defaults to the root directory.
base = "/site"

# Directory that contains the deploy-ready HTML files and assets generated by
# the build. This is relative to the base directory if one has been set, or the
# root directory if a base has not been set. This sample publishes the
# directory located at the absolute path "root/project/build-output"
# Also see: https://www.gatsbyjs.com/docs/conceptual/overview-of-the-gatsby-build-process/
command = "printenv NAME"
publish = "public/"

# We can use the .env for these variables
# [build.environment]
# API_URL = "https://"
# COGNITO_APP_CLIENT_ID = ""
# COGNITO_REGION = ""
# COGNITO_USER_POOL_ID = ""
# CONTENTFUL_ACCESS_TOKEN = ""
# CONTENTFUL_ENVIRONMENT = ""
# CONTENTFUL_SPACE_ID = ""
# GATSBY_EXPERIMENTAL_PAGE_BUILD_ON_DATA_CHANGES = true

# If skip_processing = true, all other settings are ignored
[build.processing]
skip_processing = true
# [build.processing.css]
#   bundle = false
#   minify = false
# [build.processing.js]
#   bundle = false
#   minify = false
# [build.processing.html]
#   pretty_urls = false
# [build.processing.images]
#   compress = false
