To export the xbge/direct-democracy starter content from contentful run the following command in this directory:

``` shell
contentful login
contentful space export --config contentful-export-starter-config.json
```
